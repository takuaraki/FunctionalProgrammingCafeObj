mod! OPERATOR-EXAMPLE1 {
  ** 定数 (アリティが空列の演算)
  ops true false : -> Bool

  ** 中置演算子の加算
  op _+_ : Nat Nat -> Nat

  ** mix-fix の if 文
  op if_then_else_if : Bool Exp Exp -> Exp

  ** (juxtaposition) の列
  op __ : Elt Sequence -> Sequence
}